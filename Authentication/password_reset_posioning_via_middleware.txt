use of X-Forwarded-Host header 

X-Forwarded-Host (XFH) is an HTTP header that identifies the 
original host requested by the client. When web applications 
blindly trust this header for routing, caching, or URL generation 
without validation, it leads to several critical vulnerabilities, most notably 
HTTP Host Header Injection

